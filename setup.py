from setuptools import setup

setup(
    name = "Snowflake-ddl-handler",
    version="1.0.0",
    packages= [".Snowflake_ddl_handler"],
    description="This package is used for dividing Snowflake database ddl into separate objects."
)